version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
      - mvn clean prepare-package war:explode
  post_build:
    commands:
      - echo Build completed successfully
artifacts:
  files:
    - target/pension-portal.war
  discard-paths: yes
